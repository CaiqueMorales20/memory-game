// Imports
import { styled } from "styled-components";

// Styled Components
export const CardS = styled.div`
	border-radius: 50%;
	border: 0.25rem solid red;
	width: 10rem;
	aspect-ratio: 1 / 1;
	display: grid;
	place-items: center;
`;
